# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty. It contains only a `.gitkeep` placeholder and a single "Initialize repository" commit on `main`. There is no source code, build system, test suite, or configuration to document yet.

When the first code lands, update this file with:
- Build, lint, and test commands (including how to run a single test)
- High-level architecture that spans multiple files
- Any project-specific conventions that aren't obvious from the code
