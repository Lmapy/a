strategy_name: "level_sweep"
enabled: true
instruments: ["ES", "NQ", "GC"]
timeframe: "5min"
parameters:
  sweep_buffer_ticks: 2
  rejection_wick_pct: 0.40
  stop_buffer_ticks: 4
  take_profit_rr: 3.0
  max_sweep_ticks: 40
  use_pdh_pdl: true
  use_swing_levels: true
  swing_lookback_bars: 48
regime_filter:
  allowed: ["ranging", "weak_trend", "low_volatility", "high_volatility", "strong_trend_up", "strong_trend_down"]
risk:
  max_risk_per_trade_pct: 5.0
